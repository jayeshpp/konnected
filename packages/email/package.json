{
  "name": "@konnected/email",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "build:types": "tsc -p tsconfig.json --emitDeclarationOnly",
    "build:templates": "mkdir -p dist/templates && mjml './src/templates/*.mjml' -o dist/templates/",
    "build:html": "tsc -p tsconfig.json && node src/scripts/build-templates.js",
    "build:ts": "tsc -p tsconfig.json",
    "build": "pnpm run build:ts && pnpm run build:templates",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@konnected/config": "workspace:^",
    "nodemailer": "^7.0.6"
  },
  "devDependencies": {
    "@types/mjml": "^4.7.4",
    "@types/node": "^22.17.0",
    "@types/nodemailer": "^7.0.1",
    "handlebars": "^4.7.8",
    "mjml": "^4.15.3",
    "rimraf": "^6.0.1"
  }
}
