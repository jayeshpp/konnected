{
  "name": "@konnected/middlewares",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@konnected/types": "workspace:*",
    "@konnected/libs": "workspace:*",
    "fastify": "^5.3.2",
    "@fastify/jwt": "^9.1.0",
    "argon2": "^0.41.1",
    "zod": "^3.23.0"
  }
}
