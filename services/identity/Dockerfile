# services/identity/Dockerfile
FROM monorepo-base AS identity

# Set working directory to service
WORKDIR /app/services/identity

# Expose port (example: if it runs on 5001)
EXPOSE 5001

# Start the service
CMD ["pnpm", "dev"]
